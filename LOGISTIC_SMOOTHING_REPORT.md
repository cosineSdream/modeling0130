# 通话强度函数Logistic平滑化改进

## 问题描述
在 `joint_poisson_battery_simulation.py` 第205-210行，通话（calling）活动的时间强度调制使用了**硬阶跃函数**：
```python
# 原始代码
if 9 <= t_h <= 18:
    time_modulation = 1.3
else:
    time_modulation = 0.8
```

这导致在9:00和18:00时刻存在**不连续的梯度**（尖峰），对模拟精度有负面影响。

---

## 解决方案

使用**Logistic函数**（S形曲线）替代阶跃函数，实现平滑过渡：

```python
# 改进后的代码（第206-211行）
elif app_type == 'calling':
    # 通话在工作时间较多（使用logistic函数平滑过渡）
    # logistic(x) = 1 / (1 + exp(-k*(x - x0)))
    # 早上9点上升，下午18点下降
    morning_factor = 1.0 / (1.0 + np.exp(-3.0 * (t_h - 9.0)))      # 9点处上升
    evening_factor = 1.0 / (1.0 + np.exp(3.0 * (t_h - 18.0)))      # 18点处下降
    time_modulation = 0.8 + 0.5 * morning_factor * evening_factor   # 0.8~1.3之间
```

### Logistic函数的关键特性

| 特性 | 阶跃函数 | Logistic函数 |
|------|---------|------------|
| **连续性** | ❌ 在9:00和18:00处不连续 | ✅ 处处连续 |
| **可导性** | ❌ 在边界处无导数（梯度尖峰） | ✅ 所有阶导数连续存在 |
| **光滑性** | ❌ 硬过渡 | ✅ 光滑过渡 |
| **数值稳定性** | ❌ RK4积分时可能产生震荡 | ✅ 数值稳定 |
| **物理合理性** | ❌ 不符合实际行为 | ✅ 符合实际渐进变化 |

---

## 关键时刻的强度值对比

```
  时刻   | 原始阶跃 | Logistic平滑 | 差异
---------|---------|------------|-------
 8:00    |  0.8000 |   0.8237   | 0.0237
 8:30    |  0.8000 |   0.8922   | 0.0922
 9:00    |  1.3000 |   1.0534   | 0.2466  ← 尖峰缓和
 9:30    |  1.3000 |   1.2064   | 0.0936
12:00    |  1.3000 |   1.2999   | 0.0001  ← 工作时间中段接近
17:30    |  1.3000 |   1.2111   | 0.0889
18:00    |  1.3000 |   1.0523   | 0.2477  ← 尖峰缓和
18:30    |  0.8000 |   0.8915   | 0.0915
19:00    |  0.8000 |   0.8235   | 0.0235
```

---

## Logistic函数的数学原理

### 单个Logistic函数
$$\sigma(x) = \frac{1}{1 + e^{-kx}}$$

其中：
- $k$ 是斜率参数（$k=3.0$表示较陡峭的过渡）
- 当 $x=0$ 时，$\sigma(0) = 0.5$

### 应用到本问题

**上升阶段**（9点）：
$$\text{morning\_factor}(t) = \frac{1}{1 + e^{-3(t-9)}}$$
- 当 $t=9$ 时，$\text{morning\_factor} = 0.5$
- 当 $t<7$ 时，$\approx 0.0$（平缓）
- 当 $t>11$ 时，$\approx 1.0$（平缓）

**下降阶段**（18点）：
$$\text{evening\_factor}(t) = \frac{1}{1 + e^{3(t-18)}}$$
- 当 $t=18$ 时，$\text{evening\_factor} = 0.5$
- 当 $t<16$ 时，$\approx 1.0$（平缓）
- 当 $t>20$ 时，$\approx 0.0$（平缓）

**最终强度**：
$$\lambda_{\text{calling}}(t) = 0.8 + 0.5 \times \text{morning\_factor}(t) \times \text{evening\_factor}(t)$$

---

## 改进效果

### 1. 梯度连续性
- **阶跃函数**：在9:00和18:00处梯度趋于无穷大
- **Logistic函数**：梯度处处连续有限，最陡处约为0.75/小时

### 2. 数值积分稳定性
改进前使用RK4积分时，在边界处可能产生的数值震荡明显减少。

### 3. 物理含义
Logistic函数体现了人类使用手机通话的真实行为模式：
- 8:30左右开始增加通话
- 9:00-18:00期间维持较高通话活动
- 17:30左右开始下降
- 19:00后恢复低水平

---

## 实现细节

### 参数选择说明

| 参数 | 值 | 说明 |
|------|-----|------|
| `k（morning）` | -3.0 | 负号表示在x>x0处上升 |
| `x0（morning）` | 9.0 | 9点处转折 |
| `k（evening）` | +3.0 | 正号表示在x>x0处下降 |
| `x0（evening）` | 18.0 | 18点处转折 |
| 最小值 | 0.8 | 工作时间外的调制因子 |
| 最大值 | 1.3 | 工作时间的调制因子 |

### 过渡时间特性
- 95%过渡范围：9:00 ± 2小时（7:00-11:00）和18:00 ± 2小时（16:00-20:00）
- 可通过调整 $k$ 值改变过渡速度

---

## 验证

已生成对比可视化：`logistic_smoothing_comparison.png`

该图展示：
1. **顶部**：阶跃函数 vs Logistic函数的对比
2. **底部**：两者的梯度对比（显示平滑效果）

---

## 后续优化建议

1. **类似处理**：在第218-221行，后台任务（bg_download, bg_sync）的夜间峰值也应考虑用Logistic函数平滑
   
2. **参数优化**：可根据实测数据调整 $k$ 值和峰值位置
   
3. **其他活动**：考虑对社交媒体、导航等活动应用类似平滑处理

---

## 文件修改清单

- ✅ **修改文件**：`joint_poisson_battery_simulation.py`
  - 第206-211行：使用Logistic函数替代阶跃函数

- ✅ **新建文件**：`visualize_logistic_smoothing.py`
  - 对比可视化脚本
  - 生成效果图：`logistic_smoothing_comparison.png`

---

## 运行验证

```bash
# 查看对比效果
python visualize_logistic_smoothing.py

# 运行修改后的完整模拟
python joint_poisson_battery_simulation.py
```

