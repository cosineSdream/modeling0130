# 联合泊松过程电池仿真模型

## 目录
1. [模型概述](#模型概述)
2. [联合泊松过程理论](#联合泊松过程理论)
3. [核心改进](#核心改进)
4. [数学框架](#数学框架)
5. [实现细节](#实现细节)
6. [应用定义](#应用定义)
7. [仿真流程](#仿真流程)
8. [关键公式](#关键公式)
9. [代码结构](#代码结构)
10. [使用说明](#使用说明)

---

## 模型概述

本模型实现了**联合泊松过程（Joint Poisson Process）**驱动的智能手机电池仿真系统，将多个独立的应用事件流建模为独立的泊松过程，并与电化学模型（ECM）和热模型耦合。

### 主要特点

- **8种应用类型**：视频、游戏、社交、导航、通话、音乐、后台下载、后台同步
- **独立泊松过程**：每个应用有自己独立的到达率函数 λᵢ(t)
- **时间相关性**：考虑昼夜节律和应用特定使用模式
- **完整耦合**：事件驱动 → 功耗计算 → 电化学响应 → 热效应
- **老化建模**：支持5个老化水平（0 - 5000 Ah）

---

## 联合泊松过程理论

### 1. 基本概念

**联合泊松过程**是多个独立泊松过程的组合。在本模型中：

- 每个应用类型 i 对应一个独立的泊松过程
- 第 i 个应用的事件以强度 λᵢ(t) 到达
- 各应用的到达过程相互独立
- 总事件流是所有独立过程的叠加

### 2. 数学定义

对于 N 个独立的泊松过程：

```
λ_total(t) = Σ λᵢ(t)  (i=1 to N)
```

其中：
- **λᵢ(t)**: 应用 i 在时间 t 的到达强度（events/hour）
- **λ_total(t)**: 总到达强度

### 3. 独立性条件

泊松过程的独立性意味着：
- 应用 i 在区间 [t, t+Δt] 内到达 k 个事件的概率仅依赖于 λᵢ(t)
- 不同应用的到达相互独立
- P(应用i到达 ∩ 应用j到达) = P(应用i到达) × P(应用j到达)

### 4. 时间非齐次性

本模型采用**非齐次泊松过程**（Non-homogeneous Poisson Process），即 λᵢ(t) 随时间变化：

```python
λᵢ(t) = base_intensity(t) × weight_i × time_modulation_i(t)
```

---

## 核心改进

### 原始模型 vs 联合泊松模型

| 特性 | 原始模型 | 联合泊松模型 |
|------|---------|-------------|
| 事件生成 | 单一标记泊松过程 | 8个独立泊松过程 |
| 应用选择 | 事件到达后随机选择应用类型 | 每个应用独立决定是否到达 |
| 时间模式 | 统一的昼夜节律 | 每个应用有特定时间偏好 |
| 独立性 | 应用间不独立（竞争选择） | 应用间完全独立 |
| 真实性 | 较低（单一过程） | 较高（符合真实使用模式） |

### 改进优势

1. **更真实的使用模式**
   - 通话在工作时间集中
   - 视频在晚间更频繁
   - 导航在通勤时段高峰
   - 后台任务在夜间活跃

2. **独立事件流**
   - 游戏事件不影响音乐播放的到达
   - 社交媒体刷新与视频播放独立
   - 符合实际多任务场景

3. **灵活的参数调整**
   - 每个应用可独立调整基础强度
   - 可为不同应用设置不同时间模式
   - 便于模拟特定用户行为

---

## 数学框架

### 1. 到达率函数

#### 基础昼夜节律

```
base_intensity(t) = 0.5 + peak₁(t) + peak₂(t)

peak₁(t) = 8.0 × exp(-0.5 × ((t_h - 13.0) / 3.5)²)  # 午后高峰
peak₂(t) = 10.0 × exp(-0.5 × ((t_h - 21.0) / 2.5)²) # 晚间高峰
```

#### 应用特定强度

```
λᵢ(t) = base_intensity(t) × weightᵢ × modulationᵢ(t)
```

其中 `weightᵢ` 是应用的相对权重（归一化使 Σweightᵢ = 1）

#### 时间调制函数示例

**视频播放**（晚间偏好）：
```
modulation_video(t) = 1.0 + 0.5 × exp(-0.5 × ((t_h - 20.0) / 3.0)²)
```

**导航**（通勤高峰）：
```
modulation_navigation(t) = 1.0 + 0.8 × (
    exp(-0.5 × ((t_h - 8.0) / 1.5)²) +   # 早高峰
    exp(-0.5 × ((t_h - 18.0) / 1.5)²)    # 晚高峰
)
```

**通话**（工作时间）：
```
modulation_calling(t) = {
    1.3,  if 9 ≤ t_h ≤ 18
    0.8,  otherwise
}
```

### 2. 事件到达检测

在每个时间步 Δt 内，对每个应用 i：

```
p_arrive_i = λᵢ(t) × (Δt / 3600)  # 转换为秒级概率

if p_arrive_i ≤ 0.5:
    n_new_i ~ Bernoulli(p_arrive_i)
else:
    n_new_i ~ Poisson(p_arrive_i)
```

### 3. 事件持续时间

每个应用的持续时间服从**对数正态分布**：

```
D ~ LogNormal(μ, σ²)

其中：
σ² = ln(1 + (std / mean)²)
μ = ln(mean) - σ² / 2
```

### 4. 功耗计算

#### 单个事件功耗

```
P_event = P_screen(B) + P_cpu_inc(u) + P_net_inc(r) + P_gps_inc(gps_state)
```

参数：
- **B**: 屏幕亮度 [0,1]
- **u**: CPU利用率 [0,1]
- **r**: 网络传输速率 [0,1]

#### 总负载功耗

```
P_total(t) = P_base + Σ P_event_k(t)  (k ∈ active_set)
```

---

## 实现细节

### 应用类型参数结构

```python
APP_TYPES = {
    'video': {
        'label': '视频播放',
        'B_mean': 0.72,      # 屏幕亮度均值
        'B_std': 0.08,       # 屏幕亮度标准差
        'u_mean': 0.22,      # CPU利用率均值
        'u_std': 0.04,       # CPU利用率标准差
        'r_mean': 0.70,      # 网络速率均值
        'r_std': 0.10,       # 网络速率标准差
        'net_state': 'WIFI6',       # 网络状态
        'gps_state': 'off',         # GPS状态
        'dur_mean': 4800.0,  # 持续时间均值（秒）
        'dur_std': 2400.0,   # 持续时间标准差（秒）
        'lambda_weight': 0.22,  # 到达率权重
    },
    # ... 其他7个应用类型 ...
}
```

### 主仿真循环

```python
for i in range(n_steps):
    t = i * dt
    
    # Step 1: 联合泊松过程事件生成
    for app_type in APP_TYPE_KEYS:
        λ_i = lambda_intensity_per_app(t, app_type)
        p_arrive = λ_i * (dt / 3600.0)
        
        # 泊松到达检测
        n_new = sample_poisson_arrivals(p_arrive)
        
        # 生成新事件
        for _ in range(n_new):
            event = sample_event(t, app_type)
            active_set.append(event)
            arrival_counts[app_type] += 1
    
    # Step 2: 清理已结束事件
    active_set = [ev for ev in active_set if t < ev['t_end']]
    
    # Step 3: 计算总功耗
    P_load = P_base + sum(calc_event_power(ev) for ev in active_set)
    
    # Step 4: 电化学模型（RK4积分）
    soc_new, Vrc_new, T_new, ... = rk4_step_ecm(soc, Vrc, T, P_load, ...)
    
    # Step 5: 状态更新
    soc, Vrc, T = soc_new, Vrc_new, T_new
```

---

## 应用定义

### 8种应用类型详细参数

| 应用类型 | 屏幕亮度 | CPU利用率 | 网络速率 | GPS | 平均时长 | 权重 |
|---------|---------|----------|---------|-----|---------|------|
| 视频播放 | 0.72±0.08 | 0.22±0.04 | 0.70±0.10 | 关 | 80分钟 | 0.22 |
| 游戏 | 0.85±0.05 | 0.65±0.08 | 0.45±0.12 | 关 | 60分钟 | 0.15 |
| 社交媒体 | 0.60±0.10 | 0.18±0.05 | 0.35±0.08 | 关 | 40分钟 | 0.28 |
| 导航 | 0.75±0.06 | 0.35±0.06 | 0.25±0.05 | 定位 | 30分钟 | 0.10 |
| 通话 | 0.15±0.05 | 0.12±0.03 | 0.08±0.02 | 关 | 10分钟 | 0.12 |
| 音乐播放 | 0.20±0.08 | 0.08±0.02 | 0.15±0.04 | 关 | 120分钟 | 0.08 |
| 后台下载 | 0.00 | 0.15±0.03 | 0.85±0.08 | 关 | 20分钟 | 0.03 |
| 后台同步 | 0.00 | 0.05±0.01 | 0.20±0.05 | 关 | 3分钟 | 0.02 |

### 时间偏好特征

1. **视频播放**：晚间20:00高峰（在家放松）
2. **游戏**：午后15:00 + 晚间22:00双峰（午休+睡前）
3. **社交媒体**：早8:00 + 晚19:00（通勤时段）
4. **导航**：早8:00 + 晚18:00（通勤时段）
5. **通话**：工作时间9:00-18:00增强30%
6. **音乐播放**：早8:30 + 午14:00（通勤+工作）
7. **后台任务**：夜间1:00-6:00增强20%（WiFi自动更新）

---

## 仿真流程

### 完整仿真步骤

```
初始化
├── 设置随机种子（seed=42）
├── 初始化状态向量 [SOC=1.0, Vrc=0, T=25°C]
└── 清空事件集合

主循环（每10秒一步）
│
├── 1. 联合泊松事件生成
│   ├── 对每个应用类型i：
│   │   ├── 计算 λᵢ(t)
│   │   ├── 检测泊松到达
│   │   └── 生成新事件并加入active_set
│   └── 记录到达次数
│
├── 2. 事件管理
│   ├── 清理已结束事件
│   └── 统计活跃时间
│
├── 3. 功耗计算
│   ├── 计算各模块功耗（CPU/屏幕/网络/GPS）
│   └── 求和得总负载 P_load
│
├── 4. 电化学-热耦合求解（RK4）
│   ├── 计算ECM参数（R0, R1, C1）
│   ├── 计算OCV（考虑温度和老化）
│   ├── 恒功率求解放电电流
│   ├── 计算内部损耗 P_loss
│   ├── 热模型：dT/dt = [Q_src - h(T-Tamb)] / Cth
│   └── RK4数值积分
│
├── 5. 状态更新
│   ├── SOC更新
│   ├── RC电压更新
│   ├── 温度更新
│   └── 记录时间序列
│
└── 6. 终止条件检查
    └── if SOC < 0.05: break

输出结果
├── 时间序列（SOC, T, Vt, IL等）
├── 事件日志
├── 各应用到达统计
└── 性能指标（放电时间、最高温度等）
```

---

## 关键公式

### 1. 开路电压（OCV）

```
Voc(SOC, T, Q_aging) = p₁(T,q) × [exp(λ₁(T)×q) - 1]
                      + p₂(T,q) × [exp(λ₂(Q_aging)×q) - 1]
                      + 4.2

其中：
q = (1 - SOC) × Qeff  # 已放电量
```

参数函数：
```
p₁(T,q) = 0.5385 + 6.234×10⁻⁴T - 2.608×10⁻⁷
        + 2.191×10⁻⁵T² + 8.627×10⁻⁸Tq + 8.928×10⁻¹¹q²

p₂(T,q) = -3.189×10⁻¹¹ + 9.634×10⁻¹³T - 5.539×10⁻¹⁶Tq
        - 3.508×10⁻¹⁶T³ + 2.347×10⁻¹⁷T²q + 1.431×10⁻²⁰Tq²
        - 2.504×10⁻¹⁹T³q - 4.219×10⁻²²T²q²

λ₁(T) = 2 × (-6.573×10⁻⁵T² + 5.767×10⁻³T - 0.3226)

λ₂(Q_aging) = 2 × (1.299×10⁻¹³Q³ - 2.652×10⁻⁹Q²
                  + 3.129×10⁻⁵Q + 2.33)
```

### 2. 等效电路模型（Thevenin ECM）

```
Vt = Voc - Vrc - IL×R0

dVrc/dt = (IL×R1 - Vrc) / (R1×C1)

dSOC/dt = -IL / (Qeff × 3600)
```

### 3. 恒功率放电电流求解

```
P_load = Vt × IL

代入 Vt = Voc - Vrc - IL×R0：

P_load = (Voc - Vrc - IL×R0) × IL
       = (Voc - Vrc)IL - IL²R0

整理得二次方程：
R0×IL² - (Voc - Vrc)IL + P_load = 0

求解：
IL = [(Voc - Vrc) - √((Voc - Vrc)² - 4R0×P_load)] / (2R0)
```

### 4. 热模型

```
Cth × dT/dt = Q_src - h_th(T - Tamb)

其中热源：
Q_src = W_loss×P_loss + W_cpu×P_cpu 
      + W_screen×P_screen + W_net×P_net + W_gps×P_gps

权重系数：
W_loss = 1.0, W_cpu = 1.0, W_screen = 0.3, 
W_net = 0.7, W_gps = 0.7
```

### 5. 有效容量衰减

```
Qeff(Q_aging) = Qnominal × (1 - κ√Q_aging)

其中：
Qnominal = 5.0 Ah
κ = 1.314×10⁻³ Ah⁻⁰·⁵
```

---

## 代码结构

### 模块组织

```
joint_poisson_battery_simulation.py
│
├── 1. ECM参数拟合
│   ├── R0, R1, C1 的三次多项式拟合
│   └── 拟合质量评估（R², RMSE）
│
├── 2. 子模块功率模型
│   ├── calc_P_screen(B)    # 屏幕功率
│   ├── calc_P_cpu(u)       # CPU功率
│   ├── calc_P_net(state,r) # 网络功率
│   └── calc_P_gps(state)   # GPS功率
│
├── 3. 应用类型定义
│   └── APP_TYPES 字典（8种应用）
│
├── 4. 联合泊松过程
│   ├── lambda_intensity_per_app(t, app_type)  # 单应用强度
│   ├── lambda_total(t)                        # 总强度
│   └── sample_event(t_start, app_type)        # 事件采样
│
├── 5. OCV参数函数
│   ├── calc_p1(T, q)
│   ├── calc_p2(T, q)
│   ├── calc_lambda1(T)
│   ├── calc_lambda2(Q_aging)
│   └── calc_Qeff(Q_aging)
│
├── 6. 核心仿真
│   ├── run_joint_poisson_simulation(Q_aging, T_sim, dt)
│   │   ├── deriv_ecm()          # ECM导数函数
│   │   ├── rk4_step_ecm()       # RK4积分器
│   │   └── 主循环
│   └── 结果数据结构
│
└── 7. 可视化
    ├── 图1: 各应用独立强度 + 总强度
    ├── 图2: 多老化水平对比（SOC/T/Vt/IL）
    ├── 图3: Gantt图 + 功耗演化
    ├── 图4: 各应用到达统计 + 活跃时间占比
    └── 图5: 老化水平汇总柱状图
```

---

## 使用说明

### 1. 运行仿真

```bash
python joint_poisson_battery_simulation.py
```

### 2. 修改应用参数

编辑 `APP_TYPES` 字典中的参数：

```python
APP_TYPES['video']['lambda_weight'] = 0.30  # 增加视频到达率
APP_TYPES['gaming']['u_mean'] = 0.75        # 提高游戏CPU负载
```

### 3. 调整时间模式

在 `lambda_intensity_per_app()` 函数中修改 `time_modulation`：

```python
if app_type == 'video':
    # 自定义视频播放时间偏好
    time_modulation = 1.0 + custom_pattern(t_h)
```

### 4. 更改仿真参数

```python
# 仿真时长（默认24小时）
T_sim = 86400 * 2  # 48小时

# 时间步长（默认10秒）
dt = 5.0

# 老化水平列表
Q_AGING_LIST = [0, 200, 1000, 3000]
```

### 5. 输出数据访问

```python
# 获取新电池的仿真结果
r = all_results[0.0]

# 访问时间序列
t_hours = r['t'] / 3600.0
soc = r['soc']
temperature = r['T']

# 访问事件统计
video_arrivals = r['arrival_counts']['video']
gaming_active_time = r['app_active_time']['gaming']

# 访问性能指标
discharge_hours = r['discharge_time_h']
max_temp = r['T'].max()
```

---

## 关键改进总结

### 1. 理论层面

- **从单一标记泊松 → 联合泊松**：更符合真实多应用并行场景
- **独立性假设**：各应用到达相互独立，符合实际使用模式
- **非齐次性**：考虑昼夜节律和应用特定时间偏好

### 2. 实现层面

- **每步检测N个独立泊松过程**：替代单一到达后选择应用
- **应用特定时间调制**：每个应用有独特的时间偏好曲线
- **到达统计**：记录每个应用的到达次数，便于分析

### 3. 可视化增强

- **独立强度曲线图**：展示8条应用到达率曲线
- **到达次数柱状图**：统计各应用事件数量
- **活跃时间饼图**：展示各应用占用比例

### 4. 真实性提升

- 导航在通勤时段高频（8:00, 18:00）
- 视频在晚间高频（20:00+）
- 通话在工作时间集中（9:00-18:00）
- 后台任务在夜间活跃（1:00-6:00）

---

## 附录：联合泊松过程与实际使用场景的对应

### 场景1：上班族一天的手机使用

| 时间段 | 主要应用 | 联合泊松模型体现 |
|--------|---------|----------------|
| 7:00-9:00 | 音乐、导航、社交 | 这3个应用的λᵢ(t)同时升高 |
| 9:00-12:00 | 通话、邮件（社交） | 通话λ增强，社交λ持续 |
| 12:00-14:00 | 视频、游戏 | 午休时段视频/游戏λ小幅上升 |
| 18:00-20:00 | 导航、社交 | 下班通勤时段导航/社交λ升高 |
| 20:00-23:00 | 视频、游戏、音乐 | 晚间娱乐时段多应用λ峰值 |
| 23:00-7:00 | 后台同步/下载 | 夜间低频后台任务λ略增 |

### 场景2：多任务并行的真实性

**真实场景**：用户在听音乐的同时浏览社交媒体，突然收到来电

**联合泊松模型**：
1. t=1000s：音乐应用泊松到达，生成事件M1（持续7200s）
2. t=1500s：社交应用独立泊松到达，生成事件M2（持续2400s）
3. t=2000s：通话应用独立泊松到达，生成事件M3（持续600s）

此时 t=2000s 时刻，active_set = {M1, M2, M3}，三者并行，总功耗叠加。

---

## 参考文献

1. Ross, S. M. (2014). *Introduction to Probability Models* (11th ed.). Academic Press.
2. Kingman, J. F. C. (1993). *Poisson Processes*. Oxford University Press.
3. Plett, G. L. (2015). *Battery Management Systems, Volume I: Battery Modeling*. Artech House.

---

**文档版本**: 1.0  
**最后更新**: 2024年  
**作者**: 联合泊松电池仿真团队
