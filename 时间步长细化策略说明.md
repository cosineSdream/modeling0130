# 时间步长细化策略说明文档

## 目录
1. [问题背景](#问题背景)
2. [时间步长架构](#时间步长架构)
3. [实现原理](#实现原理)
4. [关键改进](#关键改进)
5. [参数配置指南](#参数配置指南)
6. [性能优化](#性能优化)
7. [数值精度分析](#数值精度分析)
8. [使用示例](#使用示例)

---

## 问题背景

### 原始模型的时间步长问题

在原始代码中，存在**单一时间步长**的问题：

```python
# 原始代码
dt = 10.0  # 10秒

for i in range(n_steps):
    t = i * dt
    
    # 1. 事件检测（使用dt）
    p_arrive = lambda_i(t) * (dt / 3600.0)
    
    # 2. ODE求解（也使用同一个dt）
    soc_new, Vrc_new, T_new = rk4_step(soc, Vrc, T, dt)
```

### 存在的问题

1. **数值精度不足**
   - 热模型时间常数 τ = Cth/h_th = 40/20 = 2秒
   - 使用dt=10秒会导致热响应采样不足（每个时间常数仅5个点）
   - 快速温度变化可能被漏掉

2. **事件检测粗糙**
   - 高频事件可能在同一时间步内多次到达
   - 10秒内功耗变化被平均化

3. **刚性问题风险**
   - 当dt远大于系统最小时间常数时，RK4可能不稳定
   - 虽然热模型使用了半隐式方法，但耦合系统仍有风险

---

## 时间步长架构

### 三层时间步长体系

新的实现采用**三层独立时间步长**架构：

```
┌─────────────────────────────────────────────────────────┐
│  Layer 1: 仿真主循环步长 (dt_sim)                       │
│  ├─ 事件检测（泊松到达）                                 │
│  ├─ 事件管理（添加/清理）                                │
│  ├─ 功耗计算                                            │
│  └─ 调用ODE积分器                                       │
└─────────────────────────────────────────────────────────┘
            │ 调用
            ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 2: ODE积分步长 (dt_ode)                          │
│  ├─ RK4多步积分（dt_sim = N × dt_ode）                  │
│  ├─ 电化学状态更新                                       │
│  └─ 热状态更新                                          │
└─────────────────────────────────────────────────────────┘
            │ 每dt_ode一步
            ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 3: 输出抽取 (output_decimation)                  │
│  └─ 每M步输出一次（减少存储）                            │
└─────────────────────────────────────────────────────────┘
```

### 参数定义

| 参数 | 符号 | 默认值 | 作用 |
|------|------|--------|------|
| 仿真步长 | dt_sim | 1.0秒 | 事件检测和功耗更新频率 |
| ODE步长 | dt_ode | 0.1秒 | RK4积分的内部步长 |
| 抽取因子 | output_decimation | 10 | 每10步输出1次数据 |

### 关系式

```
实际ODE步数 = dt_sim / dt_ode
输出间隔 = dt_sim × output_decimation
总计算步数 = (T_sim / dt_sim) × (dt_sim / dt_ode)
输出数据点 = T_sim / (dt_sim × output_decimation)
```

**示例**（24小时仿真）：
```
dt_sim = 1.0s, dt_ode = 0.1s, decimation = 10

总仿真步数 = 86400 / 1.0 = 86,400 步
每步ODE子步数 = 1.0 / 0.1 = 10 子步
总ODE计算量 = 86,400 × 10 = 864,000 次RK4
输出数据点 = 86,400 / 10 = 8,640 点（每10秒一个点）
```

---

## 实现原理

### 1. 多步RK4积分器

核心函数 `integrate_ode_multistep()` 实现：

```python
def integrate_ode_multistep(soc_in, Vrc_in, Temp_in, P_load, Q_other, dt_total):
    """
    使用细时间步长 dt_ode 从当前状态积分 dt_total 时间
    
    过程：
    1. 计算需要的子步数：n_ode_steps = ceil(dt_total / dt_ode)
    2. 实际步长：dt_actual = dt_total / n_ode_steps
    3. 循环执行 n_ode_steps 次RK4
    4. 返回终点状态
    """
    n_ode_steps = int(np.ceil(dt_total / dt_ode))
    dt_actual = dt_total / n_ode_steps
    
    s_curr = soc_in
    v_curr = Vrc_in
    T_curr = Temp_in
    
    for _ in range(n_ode_steps):
        # 标准RK4四阶积分
        ds1, dv1, dT1 = deriv_total(s_curr, v_curr, T_curr)
        
        s2 = s_curr + 0.5 * dt_actual * ds1
        v2 = v_curr + 0.5 * dt_actual * dv1
        T2 = T_curr + 0.5 * dt_actual * dT1
        ds2, dv2, dT2 = deriv_total(s2, v2, T2)
        
        s3 = s_curr + 0.5 * dt_actual * ds2
        v3 = v_curr + 0.5 * dt_actual * dv2
        T3 = T_curr + 0.5 * dt_actual * dT2
        ds3, dv3, dT3 = deriv_total(s3, v3, T3)
        
        s4 = s_curr + dt_actual * ds3
        v4 = v_curr + dt_actual * dv3
        T4 = T_curr + dt_actual * dT4
        ds4, dv4, dT4 = deriv_total(s4, v4, T4)
        
        # 更新状态（单步）
        s_curr += (dt_actual/6.0) * (ds1 + 2*ds2 + 2*ds3 + ds4)
        v_curr += (dt_actual/6.0) * (dv1 + 2*dv2 + 2*dv3 + dv4)
        T_curr += (dt_actual/6.0) * (dT1 + 2*dT2 + 2*dT3 + dT4)
    
    return s_curr, v_curr, T_curr, ...
```

### 2. 主循环结构

```python
for i in range(n_steps_sim):
    t = i * dt_sim
    
    # ══════════════════════════════════════════
    # 第一阶段：事件管理（dt_sim频率）
    # ══════════════════════════════════════════
    
    # 1. 泊松事件检测（每个应用独立）
    for app_type in APP_TYPE_KEYS:
        lam_i = lambda_intensity_per_app(t, app_type)
        p_arrive = lam_i * (dt_sim / 3600.0)  # 注意：使用dt_sim
        n_new = sample_poisson(p_arrive)
        # 生成新事件...
    
    # 2. 清理过期事件
    active_set = [ev for ev in active_set if t < ev['t_end']]
    
    # 3. 计算当前功耗（假设在dt_sim内恒定）
    P_load = P_BASE + sum(calc_event_power(ev) for ev in active_set)
    
    # ══════════════════════════════════════════
    # 第二阶段：ODE积分（dt_ode频率）
    # ══════════════════════════════════════════
    
    # 4. 从 t 到 t+dt_sim 的多步积分
    soc_new, Vrc_new, T_new, ... = integrate_ode_multistep(
        soc, Vrc, T, P_load, Q_other, dt_sim  # dt_sim作为总积分时间
    )
    # 内部自动使用dt_ode进行多步RK4
    
    # ══════════════════════════════════════════
    # 第三阶段：输出抽取
    # ══════════════════════════════════════════
    
    # 5. 每output_decimation步记录一次
    if i % output_decimation == 0:
        t_arr[output_idx] = t
        soc_arr[output_idx] = soc
        # ... 其他状态变量
        output_idx += 1
    
    # 6. 状态推进
    soc, Vrc, T = soc_new, Vrc_new, T_new
```

### 3. 关键假设

在每个仿真步 `[t, t+dt_sim]` 内：

1. **功耗恒定假设**
   - `P_load` 在整个dt_sim内保持不变
   - 合理性：dt_sim=1秒，远小于事件持续时间（通常几分钟到几小时）

2. **事件集合恒定假设**
   - `active_set` 在dt_sim内不变
   - 新事件在下一个仿真步才生效

3. **ODE连续性**
   - 虽然P_load分段恒定，但状态变量连续
   - 通过细ODE步长保证积分精度

---

## 关键改进

### 改进1：解耦事件检测与ODE求解

**原始代码**：
```python
dt = 10.0  # 单一步长

# 事件检测
p_arrive = lambda_i(t) * (dt / 3600.0)

# ODE求解（同一个dt）
soc_new = soc + dt * dsoc_dt
```

**新代码**：
```python
dt_sim = 1.0   # 事件检测步长
dt_ode = 0.1   # ODE求解步长

# 事件检测（dt_sim）
p_arrive = lambda_i(t) * (dt_sim / 3600.0)

# ODE求解（内部使用dt_ode）
for _ in range(dt_sim / dt_ode):
    soc_new = soc + dt_ode * dsoc_dt
```

### 改进2：提高热响应捕捉精度

**时间常数分析**：
```
τ_thermal = Cth / h_th = 40 / 20 = 2秒
τ_RC = R1 × C1 ≈ 0.03 × 500 = 15秒
```

**采样点数对比**：

| dt | τ_thermal内点数 | τ_RC内点数 | 评价 |
|----|----------------|-----------|------|
| 10秒 | 0.2 | 1.5 | 非常差 |
| 1秒 | 2 | 15 | 较好 |
| 0.1秒 | 20 | 150 | 优秀 |

**新架构**：
- 事件检测：dt_sim=1秒（τ_thermal内2个点）
- ODE积分：dt_ode=0.1秒（τ_thermal内20个点）✓

### 改进3：存储优化

**问题**：细步长导致数据量激增
```
原始：86400秒 ÷ 10秒 = 8,640 个数据点
细化：86400秒 ÷ 0.1秒 = 864,000 个数据点（×100）
```

**解决**：输出抽取
```python
if i % output_decimation == 0:
    # 仅每10步记录一次
    t_arr[output_idx] = t
    output_idx += 1

# 最终数据点：86400 ÷ (1.0 × 10) = 8,640 个（与原始相同）
```

### 改进4：自适应步长支持

虽然当前实现使用固定步长，但架构支持未来扩展：

```python
def integrate_ode_adaptive(soc, Vrc, T, P_load, Q_other, dt_total, tol=1e-6):
    """
    自适应步长RK4（未来扩展）
    
    思路：
    1. 尝试用dt_ode积分一步
    2. 用dt_ode/2积分两步
    3. 比较误差，决定是否接受或减小步长
    """
    pass  # 留作未来改进
```

---

## 参数配置指南

### 1. 基础配置（推荐）

适用于大多数场景：

```python
DT_SIM = 1.0              # 仿真步长：1秒
DT_ODE = 0.1              # ODE步长：0.1秒
OUTPUT_DECIMATION = 10    # 输出抽取：10

# 特点：
# - 事件检测精度高（1秒）
# - ODE求解稳定（0.1秒）
# - 输出数据量适中（每10秒一点）
```

### 2. 高精度配置

用于科研、论文发表：

```python
DT_SIM = 0.5              # 仿真步长：0.5秒
DT_ODE = 0.05             # ODE步长：0.05秒
OUTPUT_DECIMATION = 5     # 输出抽取：5

# 特点：
# - 捕捉所有快速瞬态
# - τ_thermal内40个采样点
# - 数据量较大（每2.5秒一点）
# - 计算时间 ≈ 基础配置的 4倍
```

### 3. 快速原型配置

用于算法调试、参数扫描：

```python
DT_SIM = 5.0              # 仿真步长：5秒
DT_ODE = 0.5              # ODE步长：0.5秒
OUTPUT_DECIMATION = 10    # 输出抽取：10

# 特点：
# - 计算快速
# - 适合大量参数扫描
# - 精度略有下降
# - 数据量小（每50秒一点）
```

### 4. 实时仿真配置

用于硬件在环（HIL）测试：

```python
DT_SIM = 0.1              # 仿真步长：0.1秒（实时要求）
DT_ODE = 0.01             # ODE步长：0.01秒
OUTPUT_DECIMATION = 1     # 输出抽取：1（全输出）

# 特点：
# - 满足实时性要求
# - 高频输出（10 Hz）
# - 计算负载重
# - 需要优化代码
```

### 参数选择流程图

```
开始
  │
  ├─ 需要发表论文？
  │   └─ 是 → 高精度配置
  │
  ├─ 需要实时仿真？
  │   └─ 是 → 实时配置
  │
  ├─ 大量参数扫描？
  │   └─ 是 → 快速原型配置
  │
  └─ 一般使用 → 基础配置（推荐）
```

---

## 性能优化

### 1. 计算复杂度分析

**单次仿真的计算量**：

```
总RK4次数 = (T_sim / dt_sim) × (dt_sim / dt_ode) × 4  # RK4有4个k值

示例（24小时，基础配置）：
= (86400 / 1.0) × (1.0 / 0.1) × 4
= 86,400 × 10 × 4
= 3,456,000 次导数计算
```

**对比原始代码**（dt=10秒）：
```
原始：(86400 / 10.0) × 4 = 34,560 次
新版：3,456,000 次
倍数：100倍 ← 这是精度的代价
```

### 2. 性能优化技巧

#### 技巧1：向量化计算

```python
# 不推荐：逐点计算
for t in t_array:
    lam = lambda_intensity_per_app(t, 'video')

# 推荐：向量化
lam_array = np.vectorize(lambda t: lambda_intensity_per_app(t, 'video'))(t_array)
```

#### 技巧2：缓存OCV参数

```python
# 在循环外预计算不变的量
lam2 = calc_lambda2(Q_aging)  # 仅依赖老化量，循环前计算

def deriv_ecm(...):
    # 使用外部的lam2，避免重复计算
    Voc = p1 * (exp(lam1*q) - 1) + p2 * (exp(lam2*q) - 1) + 4.2
```

#### 技巧3：JIT编译（Numba）

```python
from numba import jit

@jit(nopython=True)
def deriv_ecm_fast(s, v, T, P_load, Qeff, lam2):
    # 使用Numba加速
    # 注意：需要移除numpy多项式对象，改用系数数组
    pass

# 加速比：5-10倍
```

#### 技巧4：并行化（多老化水平）

```python
from multiprocessing import Pool

def run_single_aging(Q_ag):
    return run_joint_poisson_simulation(Q_aging=Q_ag, ...)

with Pool(processes=5) as pool:
    results = pool.map(run_single_aging, Q_AGING_LIST)
```

### 3. 内存优化

**问题**：输出数组占用大量内存

```python
# 原始：预分配整个数组
n_output = 8640
t_arr = np.zeros(n_output)       # 8 bytes × 8640
soc_arr = np.zeros(n_output)     # ...
# 总共 14 个数组 × 8640 × 8字节 ≈ 1 MB（可接受）
```

**优化**：使用生成器（流式输出）

```python
def simulate_streaming(Q_aging, dt_sim, dt_ode):
    """流式仿真，逐点yield"""
    soc, Vrc, T = 1.0, 0.0, 25.0
    
    for i in range(n_steps):
        # ... 计算 ...
        
        if i % output_decimation == 0:
            yield {
                't': t, 'soc': soc, 'T': T, ...
            }

# 使用
with open('output.csv', 'w') as f:
    for state in simulate_streaming(Q_aging=0, dt_sim=1.0, dt_ode=0.1):
        f.write(f"{state['t']},{state['soc']},{state['T']}\n")
```

---

## 数值精度分析

### 1. 截断误差

RK4方法的局部截断误差：

```
ε_local = O(dt^5)
```

全局误差：

```
ε_global = O(dt^4)
```

**对比**：

| dt | 全局误差阶 | 相对误差（归一化） |
|----|----------|------------------|
| 10秒 | O(10^4) | 1.0 |
| 1秒 | O(1^4) | 0.0001 |
| 0.1秒 | O(0.1^4) | 1e-8 |

### 2. 收敛性测试

运行不同dt_ode的仿真，比较结果：

```python
import matplotlib.pyplot as plt

dt_ode_list = [1.0, 0.5, 0.2, 0.1, 0.05]
discharge_times = []

for dt_ode in dt_ode_list:
    res = run_joint_poisson_simulation(
        Q_aging=0, dt_sim=1.0, dt_ode=dt_ode
    )
    discharge_times.append(res['discharge_time_h'])

# 绘制收敛曲线
plt.plot(dt_ode_list, discharge_times, 'o-')
plt.xlabel('dt_ode (s)')
plt.ylabel('放电时间 (h)')
plt.title('收敛性测试')
plt.xscale('log')
plt.show()

# 期望：曲线趋于平坦（收敛）
```

### 3. 质量守恒检验

电荷守恒：

```python
# 理论放电量
Q_theoretical = Qeff * (SOC_initial - SOC_final)

# 实际放电量
Q_actual = np.trapz(res['IL'], res['t']) / 3600.0

# 误差
error = abs(Q_theoretical - Q_actual) / Q_theoretical
print(f"电荷守恒误差: {error*100:.2f}%")

# 期望：< 0.1%
```

能量守恒：

```python
# 电池输出能量
E_battery = np.trapz(res['IL'] * res['Vt'], res['t'])

# 负载消耗能量
E_load = np.trapz(res['P_total'], res['t'])

# 损耗能量
E_loss = np.trapz(res['P_loss'], res['t'])

# 检验
print(f"能量平衡: {E_battery:.2f} ≈ {E_load + E_loss:.2f}")
```

---

## 使用示例

### 示例1：基础使用

```python
import numpy as np

# 设置随机种子
np.random.seed(42)

# 运行仿真
result = run_joint_poisson_simulation(
    Q_aging=0.0,          # 新电池
    T_sim=86400,          # 24小时
    dt_sim=1.0,           # 仿真步长1秒
    dt_ode=0.1,           # ODE步长0.1秒
    output_decimation=10  # 每10步输出
)

# 提取结果
t_hours = result['t'] / 3600.0
soc = result['soc']
temperature = result['T']

# 可视化
import matplotlib.pyplot as plt

fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(12, 8))

ax1.plot(t_hours, soc)
ax1.set_ylabel('SOC')
ax1.grid(True)

ax2.plot(t_hours, temperature)
ax2.set_xlabel('时间 (h)')
ax2.set_ylabel('温度 (°C)')
ax2.grid(True)

plt.show()
```

### 示例2：参数扫描

```python
# 扫描不同ODE步长
dt_ode_sweep = [1.0, 0.5, 0.2, 0.1, 0.05, 0.02]
results_sweep = []

for dt_ode in dt_ode_sweep:
    print(f"运行 dt_ode = {dt_ode}s...")
    res = run_joint_poisson_simulation(
        Q_aging=0, T_sim=86400, 
        dt_sim=1.0, dt_ode=dt_ode, 
        output_decimation=10
    )
    results_sweep.append({
        'dt_ode': dt_ode,
        'discharge_time': res['discharge_time_h'],
        'max_temp': res['T'].max(),
        'n_points': len(res['t'])
    })

# 分析
import pandas as pd
df = pd.DataFrame(results_sweep)
print(df)
```

### 示例3：多老化水平对比

```python
Q_aging_levels = [0, 100, 500, 1000, 5000]
all_results = {}

for Q_ag in Q_aging_levels:
    np.random.seed(42)  # 保证相同事件序列
    
    all_results[Q_ag] = run_joint_poisson_simulation(
        Q_aging=Q_ag,
        T_sim=86400,
        dt_sim=1.0,
        dt_ode=0.1,
        output_decimation=10
    )

# 对比
for Q_ag in Q_aging_levels:
    r = all_results[Q_ag]
    print(f"Q_aging={Q_ag:>4} Ah: "
          f"放电{r['discharge_time_h']:.2f}h, "
          f"峰值温度{r['T'].max():.2f}°C")
```

### 示例4：实时监控模式

```python
def simulate_with_callback(Q_aging, callback_fn, callback_interval=100):
    """
    带回调函数的仿真（用于实时可视化）
    
    参数:
        callback_fn: 回调函数 callback_fn(t, soc, T, ...)
        callback_interval: 每多少步调用一次
    """
    # ... 仿真循环 ...
    
    for i in range(n_steps):
        # ... 计算 ...
        
        if i % callback_interval == 0:
            callback_fn(
                t=t, soc=soc, T=T, 
                IL=IL_val, Vt=VL_val
            )
    
    return result

# 使用
def live_plot(t, soc, T, **kwargs):
    """实时更新图表"""
    # 更新matplotlib动画
    pass

simulate_with_callback(
    Q_aging=0, 
    callback_fn=live_plot, 
    callback_interval=100
)
```

---

## 总结

### 核心优势

1. **精度提升**
   - ODE求解精度提高100倍（dt: 10s → 0.1s）
   - 捕捉快速热瞬态（τ=2s）

2. **灵活性**
   - 解耦事件检测与ODE求解
   - 独立调整三个时间尺度

3. **可扩展性**
   - 支持未来自适应步长
   - 易于并行化

4. **存储优化**
   - 输出抽取机制
   - 数据量可控

### 推荐配置总结

| 场景 | dt_sim | dt_ode | decimation | 计算量 |
|------|--------|--------|-----------|-------|
| 日常使用 | 1.0s | 0.1s | 10 | 1× |
| 高精度 | 0.5s | 0.05s | 5 | 4× |
| 快速原型 | 5.0s | 0.5s | 10 | 0.04× |
| 实时仿真 | 0.1s | 0.01s | 1 | 100× |

### 注意事项

1. **dt_sim必须是dt_ode的整数倍**，否则会有舍入误差
2. **output_decimation应为整数**，确保输出均匀
3. **首次使用建议先运行短时间**（如1小时）测试性能
4. **内存受限时**，增大output_decimation或使用流式输出

---

**文档版本**: 1.0  
**最后更新**: 2024年  
**作者**: 电池仿真优化团队
